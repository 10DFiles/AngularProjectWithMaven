/*studentapp.controller('welcomeController', [ "$log", "$scope", "$http",
		"$location", function($log, $scope, $http, $location) {
			var self = $scope;

			self.listOfStudents = function() {
				$location.url('/ListOfStudent');
			}
			
			
			
			
			
			
			
			

		} ]);*/